# Marketing OS Bot

A Telegram bot that helps generate marketing ideas and plans.

## Setup
1. Copy `.env.example` to `.env` and fill your API keys.
2. Install requirements with `pip install -r requirements.txt`.
3. Run `python bot.py`.
